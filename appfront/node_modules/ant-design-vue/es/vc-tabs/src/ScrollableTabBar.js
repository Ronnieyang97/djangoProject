import { mergeProps, createVNode } from "vue";
import ScrollableTabBarNode from './ScrollableTabBarNode';
import TabBarRootNode from './TabBarRootNode';
import TabBarTabsNode from './TabBarTabsNode';
import SaveRef from './SaveRef';

var ScrollableTabBar = function ScrollableTabBar(_, _ref) {
  var attrs = _ref.attrs;
  return createVNode(SaveRef, {
    "children": function children(saveRef, getRef) {
      return createVNode(TabBarRootNode, mergeProps(attrs, {
        "saveRef": saveRef
      }), {
        default: function _default() {
          return [createVNode(ScrollableTabBarNode, mergeProps(attrs, {
            "saveRef": saveRef,
            "getRef": getRef
          }), {
            default: function _default() {
              return [createVNode(TabBarTabsNode, mergeProps(attrs, {
                "saveRef": saveRef
              }), null)];
            }
          })];
        }
      });
    }
  }, null);
};

ScrollableTabBar.inheritAttrs = false;
export default ScrollableTabBar;